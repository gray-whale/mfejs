export const DEFAULT_CONFIG_FILES = ['config.ts', 'config.js']; // 默认配置文件

export const SHORT_ENV = {
  development: 'dev',
  production: 'prod',
  test: 'test',
};

export const LOCAL_EXT = '.local';
export const WATCH_DEBOUNCE_STEP = 300;
export const DEFAULT_FRAMEWORK_NAME = 'mfe';
